# Specs

This folder is for project specs and feature specs.

Use it for:

- top-level project specs
- feature-specific specs
- revisions of a spec as scope changes

For small work, one spec may be enough. For larger projects, keep separate spec files instead of forcing everything into one document.

Start from `SPEC_TEMPLATE.md` when a new spec needs structure.

